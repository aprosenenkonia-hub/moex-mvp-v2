"use client"; import { useState } from "react"; import { useRouter } from "next/navigation"; import { apiFetch } from "@/lib/api";
export default function RegisterPage(){const router=useRouter(); const [email,setEmail]=useState("demo@moex.local"); const [password,setPassword]=useState("password"); const [error,setError]=useState(""); async function register(){try{const r=await apiFetch("/auth/register",{method:"POST",body:JSON.stringify({email,password})}); const d=await r.json(); localStorage.setItem("access_token",d.access_token); router.push("/dashboard")}catch{setError("Не удалось зарегистрироваться. Возможно, пользователь уже существует.")}} return <main className="min-h-screen flex items-center justify-center bg-slate-950 p-6"><div className="w-full max-w-md bg-slate-900 border border-slate-800 p-8 rounded-2xl"><h1 className="text-3xl font-bold">Register</h1><input className="mt-6 w-full rounded-xl bg-slate-950 border border-slate-700 p-4" value={email} onChange={e=>setEmail(e.target.value)} placeholder="Email"/><input className="mt-4 w-full rounded-xl bg-slate-950 border border-slate-700 p-4" value={password} onChange={e=>setPassword(e.target.value)} placeholder="Password" type="password"/>{error&&<p className="text-red-400 mt-4">{error}</p>}<button onClick={register} className="mt-6 w-full rounded-xl bg-blue-600 p-4 font-semibold">Create account</button></div></main>}
